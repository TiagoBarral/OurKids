Calendar
<%= form_for @expense url: wizard_path do |f| %>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date_of_birth, start_year: 1900, end_year: Date.today.year, include_blank: true %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>