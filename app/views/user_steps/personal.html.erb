<%= form_for @children, url: wizard_path do |f| %>

<%= @children.each do |child| %>
  <h1><%= child.first_name %></h1>
<% end %>
<%= f.label :first_name %>
<%= f.text_field :first_name %>

<%= f.text_field :last_name %>

<%= f.submit %>
<% end %>
